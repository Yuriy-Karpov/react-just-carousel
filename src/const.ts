import {sideEnumType} from './type';

// export const sideEnum: Record<sideEnumType, string> = {
//     LEFT: 'left',
//     RIGHT: 'right'
// };
export const sideEnum: {[key: string]: sideEnumType}  = {
    LEFT: 'left',
    RIGHT: 'right'
};

