import React from 'react';

export const SlideHeader = ({title}) => {
    return (
        <div className="SlideHeader">
            <span className="ItemBlock-title">Slide {title}</span>
        </div>
    );
};
